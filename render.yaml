services:
  - type: web
    name: necf-treasury-backend
    runtime: python3
    rootDir: backend
    
    # Updated build command to install Tesseract explicitly
    buildCommand: |
      echo "📦 Installing system dependencies..."
      apt-get update -qq
      apt-get install -y -qq tesseract-ocr tesseract-ocr-eng libgl1
      echo "✅ Tesseract installed"
      tesseract --version
      echo "📦 Installing Python dependencies..."
      pip install -r requirements.txt
      echo "✅ Build complete"
    
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
